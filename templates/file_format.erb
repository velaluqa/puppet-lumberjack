                "paths": [ 
                            <% @paths.each do |path| -%>
                                <% if path != @paths.last -%>
                                    "<%= path %>",
                                <% else -%>
                                    "<%= path %>"
                                <% end -%>
                            <% end -%>
                         ],
                "fields": { 
                                <% @fields.each do |key, val| -%>
                                     "<%= key %>": "<%= val >", 
                                <% end -%> 
                                    "key": "value" 
                          }
            },
            {
